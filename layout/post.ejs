<main>
    <% if (is_post()) { %>
    <h1>
        <%= page.title %>
    </h1>
    <% } %>
    <p>
        <i><time><%= date(page.date, 'YYYY/MM/DD HH:mm:ss' ) %></time></i>
    </p>
    <%- page.content %>
    <% if (theme.twikoo.enable || false) { %>
        <%- partial('components/twikoo') %>
    <% } %>
    <% if (theme.beaudar.enable || false) { %>
        <%- partial('components/beaudar') %>
    <% } %>
</main>